import { Method, nullable, Object, param, Property } from "./helper"
import { Collection, List, Map } from "./java"
import { BlockEntity, BlockPos, BlockState, CompoundTag, Direction, Entity, EntityType, ItemStack, Level, ResourceLocation, ServerPlayer } from "./minecraft"

const EntityArrayList = Object("EntityArrayList", {
  // TODO
})

const InventoryKJS = Object("InventoryKJS", {
  // TODO
})

